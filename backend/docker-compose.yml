services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: nxtchat
      POSTGRES_PASSWORD: nxtchat
      POSTGRES_DB: nxtchat
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
