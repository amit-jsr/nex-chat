fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.0
httpx==0.27.1
sqlalchemy==2.0.36
asyncpg==0.30.0
openai==2.45.0
pytest==8.3.4

# LangChain (added 2026-08-07 — see backend_progress.md's migration writeup). openai above
# is now used only transitively, via langchain-openai's client — nothing in backend/
# imports it directly anymore.
langchain==1.3.14
langchain-core==1.5.3
langchain-openai==1.4.1
langchain-postgres==0.0.17
# pgvector's Python client is pulled down to 0.3.6 as langchain-postgres's dependency
# (was 0.5.0) — still fine, only pgvector.sqlalchemy.Vector is used from it and that API
# is unchanged across both versions.
pgvector==0.3.6
# langchain-postgres talks to Postgres over psycopg3 (async), a separate connection pool
# from this project's own asyncpg-based SQLAlchemy engine — the two coexist deliberately,
# see memory/facts.py's _get_vectorstore().
psycopg[binary]==3.3.4
